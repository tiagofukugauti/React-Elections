// nCandidatos.forEach(function (element) {
//   var counts = {};
//   counts[element] = (counts[element] || 0) + 1;
//   for (var element in counts) {
//     console.log(element + ' = ' + counts[element]);
//   }
// });
